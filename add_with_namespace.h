#ifndef ADD_WITH_NAMESPACE
#define ADD_WITH_NAMESPACE

//? the foreward declarations of namespace identifiers also need to be in the same namespace inside the header file
namespace Add_Namespace {
    int add(int x, int y);
}

#endif